export interface Config {

}

export const enum ViewShowBranchComparison {
    Branch = 'branch',
    Working = 'working',
}

export const enum OutputLevel {
    Silent = 'silent',
    Errors = 'errors',
    Verbose = 'verbose',
    Debug = 'debug',
}